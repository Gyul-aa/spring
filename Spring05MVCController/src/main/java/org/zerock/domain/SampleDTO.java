/*
	@Data
	: setter(), getter(), toString(), equals(), 생성자 함수등 자동 생성
 */
package org.zerock.domain;

import lombok.Data;

@Data
public class SampleDTO {
	private String name;
	private int age;
}
